# LIDAR sensor serial port (required e.g. COM3 or /dev/ttyUSB0)
LIDAR_PORT=

# Baud rate (optional — auto-detected if omitted)
# X2: 115200, X4: 230400, G4: 500000
LIDAR_BAUD=

# MCP server port for SSE mode (default 11075)
MCP_PORT=11075
MCP_HOST=127.0.0.1

# Log level
LOG_LEVEL=INFO
